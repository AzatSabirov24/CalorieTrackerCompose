apply {
    from("$rootDir/compose-module.gradle")
}

android {
    namespace 'com.calorietrackercompose.onboarding_presentation'
}

dependencies {
    "implementation"(project(Modules.core))
    "implementation"(project(Modules.onboardingDomain))
}